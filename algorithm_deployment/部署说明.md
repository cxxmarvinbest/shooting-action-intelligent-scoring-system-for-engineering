# RK3588 部署说明 —— 投篮动作评分系统

## 1. 盒子端目录结构（用户 linaro）

```
/home/linaro/lanqiu/
├── app/                        # 本目录(my_modules/)下全部 .py 文件
│   ├── main.py                 # PyQt5 主程序入口
│   ├── pipeline.py             # 编排层：预处理→检测跟踪→姿态→特征→分段→可视化
│   ├── config.py               # 全局配置（路径/阈值/描黑边参数）
│   ├── common/
│   │   └── rknn_utils.py       # 共用 RKNN/YOLOv8 底层（DFL解码/NMS/letterbox/_RKNNBase）
│   ├── tracker/                # 目标检测+跟踪模块
│   │   ├── det_model.py        # 检测模型 RKNNDetModel（0=player, 1=ball）
│   │   ├── preprocess.py       # 描黑边预处理（1920x1080 -> 544x960）
│   │   └── target_selector.py  # 主球员筛选 + 篮球过滤
│   ├── pose_estimate/          # 姿态估计模块
│   │   ├── pose_model.py       # 姿态模型 RKNNPoseModel（17 关键点）
│   │   └── pose_feature.py     # 关节角度/左右侧/关键特征提取
│   ├── scoring.py              # 评分引擎（与 PC 版一致）
│   └── llm_api.py              # 豆包 API 评语（与 PC 版一致）
├── models/
│   ├── best.rknn               # 自训练检测模型（0=player, 1=ball）
│   └── yolov8n_pose.rknn       # 姿态模型（17 关键点）
├── shot_clips/                 # 29 个标准视频 shot_1.mp4 ~ shot_29.mp4（544x960）
├── learner_videos/             # 初学者视频（RTSP 相机录制，1920x1080）
└── chuxuezhe_clip_result/      # 评分输出（分段视频+逐帧图，自动生成）
```

> 运行入口固定为 `python3 main.py`（在 `app/` 即 `my_modules/` 目录下执行），
> 模块间使用绝对导入（`from tracker.det_model import ...` 等），依赖脚本目录在 sys.path 上。

## 2. 依赖安装（盒子上执行一次）

```bash
# 系统依赖
sudo apt update
sudo apt install -y python3-pip python3-pyqt5 libgl1 libglib2.0-0

# Python 依赖（rknn-toolkit-lite2 用瑞芯微官方 whl，版本与固件匹配）
pip3 install opencv-python-headless numpy scipy fastdtw requests
pip3 install rknn_toolkit_lite2-*.whl   # 瑞芯微官方包

# 若 apt 的 python3-pyqt5 版本不符，也可 pip 安装：
# pip3 install PyQt5
```

> opencv 建议用 headless 版避免 Qt 插件冲突；若盒子上没有中文字体，
> 界面中文可能显示为方块，安装：`sudo apt install fonts-noto-cjk`

## 3. 文件传输（WinSCP）

1. WinSCP 协议选 SFTP，主机填盒子 IP（与路由器同网段），用户 `linaro`
2. 上传 `rk3588/` 下所有 `.py` → `/home/linaro/lanqiu/app/`
3. 上传两个 `.rknn` 模型 → `/home/linaro/lanqiu/models/`
4. 上传 29 个标准视频 → `/home/linaro/lanqiu/shot_clips/`
5. 上传初学者视频 → `/home/linaro/lanqiu/learner_videos/`

也可用 scp：
```bash
scp -r rk3588/*.py linaro@<盒子IP>:/home/linaro/lanqiu/app/
scp shot_clips/*.mp4 linaro@<盒子IP>:/home/linaro/lanqiu/shot_clips/
```

## 4. 运行

通过 NoMachine 连上盒子桌面，或 SSH 后指定显示：

```bash
cd /home/linaro/lanqiu/app
python3 main.py            # NoMachine 桌面内直接运行
# SSH 远程启动图形界面：
# DISPLAY=:0 python3 main.py
```

在界面中「选择测试视频」→ 选 `learner_videos/` 下的初学者视频 →「开始分段智能评分」。

## 5. 描黑边预处理说明

- 标准视频：544x960 竖屏；RTSP 相机视频：1920x1080 横屏
- 处理逻辑（`preprocess.py`）：中央裁剪 612x1080 竖幅 → 缩放至 544x960
- 标准视频尺寸已是 544x960，自动跳过预处理（零开销）
- **人物不在画面正中时**，调整裁剪窗口横向偏移（正数向右移）：
  ```bash
  LQ_CROP_X_OFFSET=80 python3 main.py
  ```
  或改 `config.py` 的 `CROP_X_OFFSET`

## 6. 模型输出格式约定（重要）

两个 `.rknn` 模型的真实输出（以同目录 `.onnx` 为准，后处理在 `det_model.py` / `pose_model.py` 的 `_group_outputs` 中适配）：

| 模型 | 输出路数 | 输出内容 |
|------|---------|----------|
| 检测 best.rknn | 9 | 每尺度 3 路：box_dfl(64) + cls(2, **已 sigmoid**) + objectness(1) |
| 姿态 yolov8n-pose_int8.rknn | 4 | 3 路合并输出 box_dfl(64)+cls(1)（raw）+ 1 路已解码关键点 [1,17,3,8400] |

要点：
- 检测模型多出的 `objectness`(1 通道) 分支是冗余的（= cls 各通道之和），后处理直接忽略；
  cls 分支导出时已做 sigmoid，代码里**不能二次 sigmoid**。
- 姿态模型的 box 与 cls 是**合并**在 65 通道里的（前 64 是 box，末 1 是 cls）；
  关键点是**已解码**的（x/y 为输入图坐标、conf 已 sigmoid），无需再做 `kps_process`。
- 模型输入 640x640，由 `_RKNNBase` 构造参数 `model_w/model_h` 显式指定（默认 640x640）；
  RKNNLite 无 `get_model_attr`，不能在板端自动读取。
- rknn_lite 输出布局统一（NHWC 或 NCHW），`as_nchw` 用 box 分支（64/65 通道）自动判定并转换。

> 若换用不同导出方式的模型，先在盒子上打印 `self.rknn.inference()` 返回的各张量 shape，
> 再据此调整 `det_model.py` / `pose_model.py` 的 `_group_outputs`。

## 7. 常见问题排查

| 现象 | 排查方向 |
|------|---------|
| load_rknn 返回非 0 | 模型文件损坏/路径错；rknn 与 lite2 版本不匹配 |
| init_runtime 失败 | NPU 驱动/固件版本与 toolkit 不匹配，升级固件 |
| 检测不到人/球 | 先用板子上 rknn_model_zoo 官方 demo 单独验证模型；再查置信度阈值 |
| 评分全为默认 180° | 姿态模型无输出 → 查 pose 模型的 kps 分支解码 |
| 界面乱码/方块字 | 缺中文字体：fonts-noto-cjk |
| 豆包评语失败 | 检查盒子外网连通性；不影响其他评分项 |

## 8. 单机冒烟测试（不上界面，验证推理链路）

```bash
cd /home/linaro/lanqiu/app
python3 - <<'EOF'
from detection_tracking_rknn import VideoAnalyzer
a = VideoAnalyzer()
a.load_models()
s1, s2, _, _, rel_h, metrics = a.process_video("/home/linaro/lanqiu/shot_clips/shot_1.mp4")
print("seq1:", None if s1 is None else len(s1), "seq2:", None if s2 is None else len(s2))
print("rel_h:", rel_h, "frames:", len(metrics) if metrics else 0)
EOF
```
能打印出 seq 长度与 rel_h 即说明 RKNN 推理链路正常。
